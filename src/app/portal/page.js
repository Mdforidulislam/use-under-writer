'use client'

import BreadCum from "@/Components/Shared/BreadCum";

const page = () => {
    return (
        <div>
            <BreadCum pageName={'portal'}/>
        </div>
    );
};

export default page;